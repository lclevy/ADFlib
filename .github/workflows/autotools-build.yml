name: ADFlib autotools

on:
  push:
    branches: [ "citest" ]
  pull_request:
    branches: [ "citest" ]

jobs:
  build_and_test_ubuntu:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v3
    - name: install prereq.
      run: sudo apt-get update ; sudo apt-get install -y check
    - name: autogen
      run: ./autogen.sh
    - name: configure
      run: ./configure
    - name: make
      run: make
    - name: make check
      run: make check
    - name: make install
      run: sudo make install
    - name: check installation
      run: find /usr/local/bin ; ls /usr/local/lib/*.so*
    - name: update ldconfig
      run: sudo ldconfig
    - name: try unadf
      run: unadf -r arccsh.adf
    - name: try adf_show_metadata
      run:
        adf_show_metadata arccsh.adf && \
        adf_show_metadata arccsh.adf CSH
